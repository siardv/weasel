% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-diagnostics.R
\name{weasel_selectivity}
\alias{weasel_selectivity}
\title{Compare retained and excluded respondents on covariates}
\usage{
weasel_selectivity(
  plan_obj,
  scenario,
  vars = NULL,
  data = NULL,
  at = c("first", "mean")
)
}
\arguments{
\item{plan_obj}{Object returned by \code{\link[=weasel_plan]{weasel_plan()}}.}

\item{scenario}{Name (or unambiguous abbreviation) of the scenario.}

\item{vars}{Character vector of covariate columns to compare.
Defaults to every numeric or logical column except the id and wave
columns.}

\item{data}{Optional long-format data frame; defaults to the data
attached to \code{plan_obj}. Required when the plan was created with
\code{keep_data = FALSE}.}

\item{at}{Either \code{"first"} (value at the first observed wave in the
span) or \code{"mean"} (mean over observed waves in the span).}
}
\value{
A data frame with one row per covariate and the columns
\code{variable}, \code{n_retained}, \code{n_excluded} (respondents with a
non-\code{NA} value), \code{mean_retained}, \code{mean_excluded}, \code{diff}
(retained minus excluded), and \code{smd}, sorted by absolute SMD,
largest first.
}
\description{
Selecting respondents by participation completeness can bias a sample
when retention is related to substantive characteristics. This
diagnostic compares the respondents a scenario retains with those who
are observed in the span but excluded, covariate by covariate, and
reports the standardized mean difference (SMD) for each.
}
\details{
For every respondent, a single value per covariate is taken either
from their first observed wave inside the span (\code{at = "first"}, the
usual baseline comparison) or as the mean over their observed waves
in the span (\code{at = "mean"}). Missing item values are dropped within
each group. The SMD divides the group difference by the pooled
standard deviation \code{sqrt((sd_retained^2 + sd_excluded^2) / 2)};
absolute values around 0.1 or larger are commonly read as noteworthy
imbalance. The SMD is \code{NA} when either group has no spread.
}
\examples{
d <- generate_weasel_dummy_data(n_ids = 200, n_times = 10, seed = 1)
p <- weasel_plan(d, "id", "time", span = "core")
sel <- weasel_selectivity(p, "anchored_balanced")
weasel_print_table(sel, title = "Retained vs excluded", digits = 3)

}
