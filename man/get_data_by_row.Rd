% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-scope.R
\name{get_data_by_row}
\alias{get_data_by_row}
\title{Retrieve long-format data matching a wave-pattern row}
\usage{
get_data_by_row(i = NULL, within_span = FALSE)
}
\arguments{
\item{i}{Row index into the wave-pattern summary. Defaults to the
last-used index.}

\item{within_span}{If \code{TRUE}, only rows whose wave lies inside the
scoped \code{lower:upper} span are returned. If \code{FALSE} (default), all
rows of the matching respondents are returned, including waves
outside the span.}
}
\value{
A data frame (subset of the original long data).
}
\description{
After \code{\link[=summarize_waves]{summarize_waves()}} has grouped respondents into patterns,
this function extracts the long-format rows for every respondent who
matches a given pattern. Use \code{\link[=filter_wave_summary]{filter_wave_summary()}} first to
identify which row index to request.
}
\examples{
d <- generate_weasel_dummy_data(n_ids = 50, n_times = 8, seed = 1)
set_weasel_scope(d, "id", "time")
evaluate_weasel_scope()
reshape_to_wide()
summarize_waves()

subset1 <- get_data_by_row(1)
weasel_print_table(head(subset1), title = "Row-1 data preview")
weasel_clear_scope()

}
