% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-diagnostics.R
\name{weasel_sensitivity}
\alias{weasel_sensitivity}
\title{Sweep selection tolerances and count retained respondents}
\usage{
weasel_sensitivity(
  plan_obj,
  require_endpoints = c(TRUE, FALSE),
  max_missing = 0:3,
  n_gap_max = 0:2,
  max_gap_len = 0:2,
  max_gap_max = NULL
)
}
\arguments{
\item{plan_obj}{Object returned by \code{\link[=weasel_plan]{weasel_plan()}}.}

\item{require_endpoints}{Logical values to evaluate. Must be actual
logical \code{TRUE}/\code{FALSE} values; numeric or character stand-ins
(for example \code{2} or \code{"TRUE"}) are rejected rather than coerced.}

\item{max_missing}{Integer tolerances for missing waves in the span.}

\item{n_gap_max}{Integer tolerances for the number of interior gaps.}

\item{max_gap_len}{Integer tolerances for the longest interior gap.}

\item{max_gap_max}{Deprecated alias for \code{max_gap_len} (classed
warning \code{weasel_deprecated}); ignored when \code{max_gap_len} is
supplied explicitly.}
}
\value{
A data frame with one row per combination and the columns
\code{require_endpoints}, \code{max_missing}, \code{n_gap_max}, \code{max_gap_len},
\code{n_ids}, \code{prop_ids} (share of all respondents observed in the
span), and \code{mean_prop_present} (coverage among the retained
respondents; \code{NA} when nobody is retained).
}
\description{
Evaluates every combination of the supplied tolerance values against
the per-respondent metrics stored in a plan, reporting how many
respondents each combination would retain. Because the sweep reuses
the metrics that \code{\link[=weasel_plan]{weasel_plan()}} already computed, it is fast even
for large panels and works when the plan was created with
\code{keep_data = FALSE}. Use it to show how sensitive the sample size is
to the chosen tolerances before committing to a scenario.
}
\examples{
d <- generate_weasel_dummy_data(n_ids = 150, n_times = 10, seed = 1)
p <- weasel_plan(d, "id", "time", span = "core")
sens <- weasel_sensitivity(p, max_missing = 0:2)
weasel_print_table(sens, title = "Tolerance sweep", n = 12)

}
