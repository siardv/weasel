% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-dummy-data.R
\name{generate_weasel_dummy_data}
\alias{generate_weasel_dummy_data}
\title{Generate synthetic longitudinal panel data}
\usage{
generate_weasel_dummy_data(
  n_ids = 1000,
  n_times = 13,
  n_vars = 5,
  prop_random = 0.05,
  prop_attention = 0.08,
  attention_center = 10,
  attention_scale = 2.5,
  prop_attrition = 0.06,
  prop_block = 0.04,
  block_duration_range = c(2, 4),
  prop_item_missing = 0.02,
  id_start = 800001,
  waves = NULL,
  seed = NULL
)
}
\arguments{
\item{n_ids}{Number of respondents.}

\item{n_times}{Number of time points (waves). Ignored when \code{waves}
is supplied.}

\item{n_vars}{Number of outcome variables to generate.}

\item{prop_random}{Probability that any given (respondent, wave)
observation is skipped at random. Like every \code{prop_*} parameter,
it must be a single number in \code{[0, 1]}; invalid values fail
immediately, before any random draw.}

\item{prop_attention}{Asymptotic probability of attention-related
wave skipping at late waves.}

\item{attention_center}{Wave position at which the attention-decay
curve is centred. Must be a single finite number.}

\item{attention_scale}{Steepness of the attention-decay curve;
larger values give a more gradual increase. Must be a single
finite number greater than zero.}

\item{prop_attrition}{Proportion of respondents who permanently drop
out from a random wave onwards.}

\item{prop_block}{Proportion of respondents who miss one contiguous
block of waves.}

\item{block_duration_range}{Integer vector of length 2 giving the
min/max duration of block missingness.}

\item{prop_item_missing}{Probability that an individual outcome value
on an observed row is \code{NA} (item nonresponse).}

\item{id_start}{Starting integer for respondent identifiers.}

\item{waves}{Optional integer vector of wave labels, for example
\code{seq(2008, 2032, by = 2)} for a biennial schedule. When supplied it
overrides \code{n_times}; the participation mechanisms operate on the
positions of this schedule, and the returned \code{time} column contains
these labels. Analyse such data with \code{grid = "observed"} in
\code{\link[=weasel_plan]{weasel_plan()}} or \code{\link[=set_weasel_scope]{set_weasel_scope()}}.}

\item{seed}{Random seed; a single integer-valued number (fractional
and non-numeric values are rejected before \code{set.seed()} is
reached). If \code{NULL}, a seed is drawn and reported so the data set
can be regenerated. A given seed reproduces the same panel
regardless of the caller's \code{RNGkind()}; see Details.}
}
\value{
A data frame in long format with columns \code{id}, \code{time}, and
\code{var1} ... \code{varN}. Respondents keep at least one observed wave, so
the number of distinct ids always equals \code{n_ids}, but
\code{nrow(result)} is typically smaller than \code{n_ids * n_times}.
}
\description{
Creates a long-format data frame in which wave-level missingness is
represented the way real panel files represent it: a respondent who
misses a wave simply has no row for that wave. Four participation
mechanisms are layered (random skips, attention decay, attrition,
and block dropout), and a small amount of item-level missingness
(\code{NA} values inside the outcome columns of otherwise observed rows)
can be added on top.
}
\details{
The returned grid is therefore deliberately incomplete; this is what
both weasel pipelines analyse. The function has no net effect on the
caller's RNG state: the state present before the call (including the
\code{RNGkind()} setting) is restored on exit, and reproducibility is
controlled through \code{seed}. Internally the generator pins the RNG
configuration to \code{RNGkind("Mersenne-Twister", "Inversion", sample.kind = "Rejection")}, the R >= 3.6 defaults, so a given seed
reproduces the same panel even when the caller uses a non-default
sampler (for example \code{sample.kind = "Rounding"}).
}
\examples{
d <- generate_weasel_dummy_data(n_ids = 50, n_times = 8, seed = 1)

# the grid is incomplete: wave-level missingness is row absence
nrow(d) < 50 * 8

# scope pipeline starts here
set_weasel_scope(d, "id", "time")
weasel_clear_scope()

# plan pipeline starts here
p <- weasel_plan(d, "id", "time", span = "core")

# a biennial schedule; analyse with grid = "observed"
b <- generate_weasel_dummy_data(n_ids = 40, waves = seq(2008, 2020, 2),
                                seed = 1)
pb <- weasel_plan(b, "id", "time", span = "full", grid = "observed")

}
